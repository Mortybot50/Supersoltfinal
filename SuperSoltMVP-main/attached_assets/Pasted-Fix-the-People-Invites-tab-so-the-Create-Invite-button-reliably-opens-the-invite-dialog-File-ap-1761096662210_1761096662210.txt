Fix the People → Invites tab so the Create Invite button reliably opens the invite dialog.

File: app/people/page.tsx (or wherever the People page lives).

Do not gate rendering of the <Dialog> or the trigger on loaded data; render them unconditionally.

Implement a controlled shadcn dialog:

"use client";
import { useState } from "react";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogFooter, DialogTrigger } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { useMutation, useQueryClient } from "@tanstack/react-query";

function CreateInviteButton() {
  const qc = useQueryClient();
  const [open, setOpen] = useState(false);
  const [email, setEmail] = useState("");
  const [role, setRole] = useState<"manager"|"supervisor"|"crew">("manager");

  const createInvite = useMutation({
    mutationFn: async () => {
      const r = await fetch("/api/people/invites", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ email, role }),
      });
      if (!r.ok) throw new Error((await r.json()).error || "Failed to create invite");
      return r.json();
    },
    onSuccess: () => {
      qc.invalidateQueries({ queryKey: ["invites"] });
      setOpen(false);
      setEmail("");
      setRole("manager");
    },
  });

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>
        <Button>+ Create Invite</Button>
      </DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Create Invite</DialogTitle>
        </DialogHeader>
        <div className="space-y-4">
          <div className="grid gap-2">
            <Label htmlFor="invite-email">Email</Label>
            <Input
              id="invite-email"
              type="email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              placeholder="name@example.com"
            />
          </div>
          <div className="grid gap-2">
            <Label htmlFor="invite-role">Role</Label>
            <select
              id="invite-role"
              className="bg-background border rounded px-3 py-2"
              value={role}
              onChange={(e) => setRole(e.target.value as any)}
            >
              <option value="manager">manager</option>
              <option value="supervisor">supervisor</option>
              <option value="crew">crew</option>
            </select>
          </div>
        </div>
        <DialogFooter>
          <Button
            onClick={() => createInvite.mutate()}
            disabled={!email || createInvite.isPending}
          >
            {createInvite.isPending ? "Creating…" : "Create Invite"}
          </Button>
        </DialogFooter>
      </DialogContent>
    </Dialog>
  );
}


Use this component in the Invites tab header (right side), replacing the current button:

<div className="flex items-center justify-between">
  <h3 className="text-lg font-medium">Pending Invites</h3>
  <CreateInviteButton />
</div>


Ensure the Invites list query key is ["invites"] so invalidateQueries refreshes the table.

Keep role checks (owner/manager) server-side only; the button should still render (disable it if the GET /api/people/invites call told us the user isn’t allowed).

Test: Clicking Create Invite opens the dialog, submitting creates the invite (200/201), dialog closes, list refreshes.