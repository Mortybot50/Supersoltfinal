PROMPT 7 / 20 — One dashboard summary (align KPIs, chart, and top products; fix missing days; include forecast $ + qty)

Implement a single, canonical data source for the dashboard and wire the UI to it. Do all steps exactly. Do not remove existing files; replace dashboard calls to use the new endpoint.

1) Add a shared date-window helper

Create: lib/date-window.ts

Export getWindow(period: "day"|"week"|"month", startISO: string) that returns:

type Window = { start: Date; end: Date; days: number; label: string; };


Rules:

Week = Mon–Sun.

Month = calendar month containing startISO.

Use local timezone consistently (zero out to 00:00:00.000; set end to 23:59:59.999).

Also export eachDay(start: Date, end: Date): string[] returning ISO YYYY-MM-DD for every calendar day (inclusive). We’ll use this to fill gaps so the chart never drops days.

2) Create a single summary endpoint

Create: app/api/dashboard/summary/route.ts

export const runtime = "nodejs";
import { NextResponse } from "next/server";
import { requireOrg } from "@/lib/authz";
import { db } from "@/db";
import { getWindow, eachDay } from "@/lib/date-window";
// import your tables (e.g., daily_sales, sales_forecasts, menu_items, recipes, recipe_lines, shifts, staff, etc.)

export async function GET(req: Request) {
  try {
    const { venueId } = await requireOrg(); // keep tenant isolation
    const url = new URL(req.url);
    const period = (url.searchParams.get("period") || "week") as "day"|"week"|"month";
    const startISO = url.searchParams.get("start") || new Date().toISOString();

    const { start, end, days } = getWindow(period, startISO);
    const dayKeys = eachDay(start, end); // ["YYYY-MM-DD", ...]

    // 1) Ensure forecast coverage for the window (idempotent; ignore network errors)
    const qs = new URLSearchParams({ start: start.toISOString(), days: String(days) });
    fetch(`${process.env.NEXT_PUBLIC_APP_URL ?? ""}/api/forecast/generate?${qs}`, { method: "POST", cache: "no-store" }).catch(()=>{});

    // 2) Actuals per day (cents)
    // Replace with your project’s query helpers or drizzle queries.
    // Must produce Map<YYYY-MM-DD, number>
    const actualByDay = new Map<string, number>();
    // ... fill actualByDay with 0 default later

    // 3) Forecast per day (qty & cents)
    // Build by summing item qty forecasts, then multiply by current price_cents
    const forecastQtyByDay = new Map<string, number>();
    const forecastCentsByDay = new Map<string, number>();
    // ... fill both maps; default 0 later

    // 4) KPIs over the window
    // - salesCents = sum(actualByDay)
    // - cogsPct: theoretical COGS based on recipes * actual qty in window (nullable if data incomplete)
    // - labourPct: scheduled labour cost / salesCents (or null if no sales and we avoid forecast-based % here)
    const kpis = {
      salesCents: 0,
      cogsPct: null as number | null,
      labourPct: null as number | null,
    };

    // 5) Top products for the window with change vs previous window
    // Output: [{ id, name, valueCents, pctOfSales, changePct }]
    const topProducts: { id:string; name:string; valueCents:number; pctOfSales:number; changePct:number }[] = [];

    // ===== IMPLEMENTATION NOTES =====
    // - Replace the ellipses with your actual queries.
    // - Always default missing days to 0 (see loop below).
    // - For topProducts:
    //    * thisWindow = sum actual revenue by item within [start..end]
    //    * prevWindow = same length immediately before start
    //    * changePct = ((this - prev) / max(prev,1)) * 100 (1 decimal in UI)
    // - For COGS%: formula = (theoretical_ingredient_cost / actual_sales) * 100; nullable if denominator 0 or recipes missing.
    // - For labour%: use scheduled wage snapshots if you have them for the window; nullable if missing.

    // ===== DEFAULT-FILL DAYS =====
    const chartDays = dayKeys.map((d) => {
      const ac = actualByDay.get(d) ?? 0;
      const fc = forecastCentsByDay.get(d) ?? 0;
      const fq = forecastQtyByDay.get(d) ?? 0;
      return { date: d, actualCents: ac, forecastCents: fc, forecastQty: fq };
    });

    // Sum for salesCents if not yet computed
    if (kpis.salesCents === 0) {
      kpis.salesCents = chartDays.reduce((s, r) => s + r.actualCents, 0);
    }

    return NextResponse.json({ kpis, chart: { days: chartDays }, topProducts }, { status: 200 });
  } catch (e:any) {
    return NextResponse.json({ error: e?.message ?? "summary_error" }, { status: e?.statusCode ?? 500 });
  }
}


Requirements for the implementation inside the comments (you must complete them):

Actuals: sum actual revenue by day for the venue in [start..end].

Forecasts: from sales_forecasts per item per day → sum qty per day → multiply by current menu_items.price_cents to get forecastCents.

Top Products: compute current window revenue per item; compute previous window revenue per item; return changePct for each of the top 10 by value.

Never 400 for “no data”. Always 200 with zeros/empty arrays.

Keep Node runtime (already declared).

Ensure you use existing indexes or add them if missing:

sales_forecasts (venue_id, date)

daily_sales / sales (venue_id, date) (adjust to your table)

menu_items (venue_id, id)

3) Wire the dashboard to this single endpoint

Edit: app/dashboard/DashboardClient.tsx (client)

Replace multiple fetches with one call to /api/dashboard/summary?period=…&start=….

Use the existing DateNav to control period + start.

Render:

KPI cards from summary.kpis.

Chart from summary.chart.days (ensure the chart component expects { date, actualCents, forecastCents, forecastQty }).

Top Products table from summary.topProducts (display changePct with ↑/↓ and one decimal).

Keep the chart as a dynamic import (ssr: false), wrap in Suspense with a skeleton.

4) Chart component update

Edit: components/charts/SalesVsForecast.tsx (or your chart file)

Accept the normalized array [{ date, actualCents, forecastCents, forecastQty }].

Show two bars per day: Actual ($) and Forecast ($); tooltip includes a second line “Forecast qty: X”.

Do not drop missing days; you’ll receive a full calendar array.

5) Clean up & guarantees

Stop using any separate dashboard KPI/forecast endpoints on the dashboard page.

All dashboard numbers (KPI totals, chart sums, top products total) must be derived from this single endpoint/window.

Ensure this endpoint always returns in <300ms on dev data; optimize with batched queries and Promise.all.

6) Acceptance checklist (run E2E)

Switching Day/Week/Month shows no missing days on the chart.

KPI Sales equals the sum of chart Actual bars for the selected window.

Future windows show non-zero forecast bars; actuals remain 0.

Top Products table shows realistic % of sales and change% vs previous window (not ~0.0% unless it really is).

The endpoint returns 200 with empty arrays when there’s no data (never 400).